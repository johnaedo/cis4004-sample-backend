info:
  name: Register New User
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}users/register"
  body:
    type: json
    data: |-
      {
        "username":"mickey",
        "email":"mouse@disney.com",
        "password":"eigersux"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
