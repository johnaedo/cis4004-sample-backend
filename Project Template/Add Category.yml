info:
  name: Add Category
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}categories"
  body:
    type: json
    data: |-
      {
        "user":"jaedo",
        "name":"Electronics",
        "type":"expense",
        "color":"#f0392b"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
