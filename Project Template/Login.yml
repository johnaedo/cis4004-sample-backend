info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}users/login"
  body:
    type: json
    data: |-
      {
        "identifier":"jaedo",
        "password":"ucf123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
