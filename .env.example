# Environment
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/budget_planner
# MONGODB_ADMIN_URI is optional, used only to provision the app user
# MONGODB_ADMIN_URI=mongodb://root:rootpass@localhost:27017/admin

# Optional credentials (used by resetDatabase.js to provision/configure the app database)
DB_USER=budget_user
DB_PASSWORD=cis4004
DB_NAME=budget_planner

# Server Configuration
PORT=3001

# Security
JWT_SECRET=your-secret-key-change-this